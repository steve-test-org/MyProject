---
version: 0.2

phases:
  install:
    commands:
      - pip install sceptre
  build:
    commands:
      - echo "Building..."
      - ENV | grep AccountID
      - ENV | grep EnvironmentName
      # - sceptre --var env=${ENV} create dev/vpc.yaml -y
